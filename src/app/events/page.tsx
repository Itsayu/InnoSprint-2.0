import { PageContainer } from '@/components/page-container';

export default function EventsPage() {
    return (
        <PageContainer title="Events & Schedule">
            <p>The saga unfolds with intense coding challenges, mentorship from industry titans, and workshops on forbidden technologies. A full schedule of events will be revealed soon. Prepare for battle.</p>
        </PageContainer>
    );
}
